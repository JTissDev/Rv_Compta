# API Server README (English draft)

Module: api-server
Role: Spring Boot REST API that exposes the engine features. This module is the planned pivot for integrations.

How to run
```bash
cd api-server
mvn spring-boot:run
```

Notes
- Add integration tests for controller exception mapping and DTO serialization.
